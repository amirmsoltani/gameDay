import SkillPage from '@/components/dashboard/skills';
import AppLayout from '@/layout/app-layout';
import React from 'react';

const Skills = () => {
    return (
        <AppLayout>
            <SkillPage />
        </AppLayout>
    );
};

export default Skills;
