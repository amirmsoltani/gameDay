import React from 'react';
import AppLayout from '@/layout/app-layout';

const Dashboard = () => {
    return (
        <AppLayout>
                test
        </AppLayout>
    );
};

export default Dashboard;