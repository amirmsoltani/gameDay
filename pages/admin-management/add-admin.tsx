import AddAdmin from '@/components/admin-management/add-admin';
import AppLayout from '@/layout/app-layout';
import React from 'react';

const AddAdmins = () => {
    return (
        <AppLayout>
            <AddAdmin />
        </AppLayout>
    );
};

export default AddAdmins;
