import NewCoursePage from '@/components/catalog/new-course';
import AppLayout from '@/layout/app-layout';
import React from 'react';

const Course = () => {
    return (
        <AppLayout>
            <NewCoursePage />
        </AppLayout>
    );
};

export default Course;
