import NewSkillPage from '@/components/catalog/new-skill';
import AppLayout from '@/layout/app-layout';
import React from 'react';

const Skill = () => {
    return (
        <AppLayout>
            <NewSkillPage />
        </AppLayout>
    );
};

export default Skill;
