query getCategories(
    $take: Int
    $skip: Int
    $where: SkillCategoryFilterInput
    $order: [SkillCategorySortInput!]
) {
    skillcategory_getSkillCategories {
        result(take: $take, skip: $skip, where: $where, order: $order) {
            items {
                id
                title
                rate
                iconUrl
                lessons {
                    id
                }
                comments {
                    status
                }
            }
            pageInfo {
                hasNextPage
            }
            totalCount
        }
        status
    }
}

query getLesson($where: LessonFilterInput, $take: Int, $skip: Int) {
    lesson_getLessons {
        result(where: $where, take: $take, skip: $skip) {
            items {
                id
                title
                description
                time
                topics {
                    id
                    title
                    description
                    fileUrl
                    isDeleted
                    isMain
                    createdDate
                    lessonId
                }
            }
            pageInfo {
                hasNextPage
            }
            totalCount
        }
        status
    }
}

query getCatalogSkill($where: SkillFilterInput, $take: Int, $skip: Int) {
    skill_getSkills {
        result(where: $where, take: $take, skip: $skip) {
            items {
                id
                iconUrl
                title
                skillCategory {
                    title
                }
                skillQuestions {
                    id
                }
            }
            pageInfo {
                hasNextPage
            }
            totalCount
        }
        status
    }
}
